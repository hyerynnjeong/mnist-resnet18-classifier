# mnist-resnet18-classifier
PyTorch implementation of a ResNet18 model for MNIST digit classification, including data preprocessing, training, and performance evaluation using classification metrics.
